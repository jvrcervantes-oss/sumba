# Bloquea cualquier acceso web directo a esta carpeta (Apache 2.4 y 2.2)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
